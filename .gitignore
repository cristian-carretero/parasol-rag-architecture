# Virtual environment
.venv/
venv/

# Python cache
__pycache__/
*.pyc

# Data (do not version heavy files)
data/

# Generated figures — heavy, regenerable from the pipeline
outputs/figures/

# Diagnostics (JSON rule trees, audit summaries) are versioned:
# they are small text reports and serve as a historical reference of each run.
# No ignore rule needed for outputs/diagnostics/.

# System or IDE files
.DS_Store
.vscode/
.idea/

# Streamlit agent skills (environment-specific symlinks)
.agents/skills/outputs/diagnostics/
